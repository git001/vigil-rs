#!/bin/sh
# Wrapper for HAProxy — called by vigild.
# vigild sends SIGUSR1 on stop, which HAProxy interprets as graceful drain
# (stop accepting new connections, finish in-flight requests, then exit).
set -e

mkdir -p /var/run/haproxy

exec haproxy -W -db -f /etc/haproxy/haproxy.cfg
